
SSH

    $ mkdir -p ~/tmp ~/.ssh
    $ chmod 700 ~/.ssh
    $ install -m600 home_ssh_config ~/.ssh/config

X resources

    # install -o root -g root -m 644 -t /usr/share/X11/app-defaults/ \
      x11_app-defaults/*


Profile & Shell

    # install -o root -g root -m 644 -t /etc etc/*
