
SSH

    $ mkdir -p ~/tmp ~/.ssh
    $ chmod 700 ~/.ssh
    $ install -m600 home_ssh_config ~/.ssh/config

    # rsync --chmod=644 \
      x11_app-defaults_SshAskpass /usr/share/X11/app-defaults/SshAskpass


XTerm

    # rsync --chmod=644 XTerm XTerm-color /usr/share/X11/app-defaults/


Profile & Shell

    # rsync --chmod 644 etc_dircolors /etc/dircolors

    # rsync --chmod 644 etc_bash.bashrc /etc/bash.bashrc
    # rsync --chmod 644 etc_profile /etc/profile

Tmux

    # rsync --chmod 644 etc_tmux.conf /etc/tmux.conf
